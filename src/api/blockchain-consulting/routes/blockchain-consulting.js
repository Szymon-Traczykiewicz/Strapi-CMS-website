'use strict';

/**
 * blockchain-consulting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::blockchain-consulting.blockchain-consulting');
